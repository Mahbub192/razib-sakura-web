# Sakura Healthcare Management System - Complete Flow Chart

## 📋 Table of Contents
1. [Authentication Flow](#authentication-flow)
2. [Patient Flow](#patient-flow)
3. [Doctor Flow](#doctor-flow)
4. [Assistant Flow](#assistant-flow)
5. [Admin Flow](#admin-flow)
6. [Appointment Booking Flow](#appointment-booking-flow)
7. [System Integration Flow](#system-integration-flow)

---

## 🔐 Authentication Flow

```
┌─────────────────────────────────────────────────────────────────┐
│                        USER AUTHENTICATION                        │
└─────────────────────────────────────────────────────────────────┘

                    ┌──────────────┐
                    │   Home Page  │
                    └──────┬───────┘
                           │
            ┌──────────────┴──────────────┐
            │                              │
    ┌───────▼───────┐            ┌─────────▼────────┐
    │    Register    │            │      Login       │
    └───────┬───────┘            └─────────┬────────┘
            │                               │
            │  POST /api/auth/register      │  POST /api/auth/login
            │                               │
    ┌───────▼───────────────────────────────▼───────┐
    │         Backend Authentication Service        │
    │  - Validate credentials                       │
    │  - Hash password (bcrypt)                     │
    │  - Generate JWT token                          │
    │  - Store user in database                      │
    └───────┬───────────────────────────────────────┘
            │
            │  Response: { token, user, role }
            │
    ┌───────▼───────────────────────────────────────┐
    │         Frontend Token Management              │
    │  - Store token in localStorage                 │
    │  - Store user data in localStorage             │
    │  - Set authentication state                    │
    └───────┬───────────────────────────────────────┘
            │
    ┌───────▼───────────────────────────────────────┐
    │         Role-Based Redirection                 │
    └───────┬───────────────────────────────────────┘
            │
    ┌───────┴───────┬──────────────┬──────────────┐
    │               │              │              │
┌───▼───┐    ┌─────▼─────┐  ┌────▼─────┐  ┌────▼─────┐
│Patient│    │   Doctor   │  │Assistant │  │  Admin   │
│Dashboard│  │  Dashboard │  │ Dashboard│  │ Dashboard│
└────────┘    └───────────┘  └──────────┘  └──────────┘
```

### Authentication Features:
- ✅ Registration (phone/email, password)
- ✅ Login (phone/email, password)
- ✅ Forgot Password (OTP verification)
- ✅ Reset Password
- ✅ Change Password
- ✅ JWT Token Management
- ✅ Role-Based Access Control (RBAC)

---

## 👤 Patient Flow

```
┌─────────────────────────────────────────────────────────────────┐
│                          PATIENT DASHBOARD                       │
└─────────────────────────────────────────────────────────────────┘

                    ┌─────────────────┐
                    │ Patient Login   │
                    └────────┬────────┘
                             │
                    ┌────────▼────────┐
                    │ Patient Dashboard│
                    │  - Greeting      │
                    │  - Upcoming Appt │
                    │  - Recent Msgs   │
                    │  - Quick Links    │
                    └────────┬─────────┘
                             │
        ┌────────────────────┼────────────────────┐
        │                    │                    │
┌───────▼───────┐  ┌─────────▼─────────┐  ┌─────▼──────┐
│   Appointments│  │  Medical Records  │  │ Lab Results│
└───────┬───────┘  └─────────┬─────────┘  └─────┬──────┘
        │                    │                   │
        │                    │                   │
┌───────▼───────┐  ┌─────────▼─────────┐  ┌─────▼──────┐
│  Prescriptions│  │     Messages       │  │   Profile  │
└───────┬───────┘  └─────────┬─────────┘  └─────┬──────┘
        │                    │                   │
        │                    │                   │
        └────────────────────┴───────────────────┘
                             │
                    ┌────────▼────────┐
                    │  Book Appointment│
                    └────────┬─────────┘
                             │
                    ┌────────▼────────┐
                    │  Select Doctor   │
                    │  Select Clinic   │
                    │  Select Date     │
                    │  Select Time     │
                    │  Enter Reason    │
                    └────────┬────────┘
                             │
                    ┌────────▼────────┐
                    │ POST /api/    │
                    │ patients/:id/  │
                    │ appointments   │
                    └────────┬────────┘
                             │
                    ┌────────▼────────┐
                    │  Slot Marked    │
                    │  as BOOKED      │
                    └─────────────────┘
```

### Patient Features:
- ✅ Dashboard with personalized greeting
- ✅ Book Appointment (with slot selection)
- ✅ View Appointments (Upcoming/Past)
- ✅ Reschedule Appointment
- ✅ Cancel Appointment
- ✅ View Medical Records (with filters)
- ✅ View Lab Results (with status colors)
- ✅ View Prescriptions (Active/Past, Refill request)
- ✅ Messages (Conversations, Chat)
- ✅ Profile Management (with photo upload)

---

## 👨‍⚕️ Doctor Flow

```
┌─────────────────────────────────────────────────────────────────┐
│                          DOCTOR DASHBOARD                        │
└─────────────────────────────────────────────────────────────────┘

                    ┌─────────────────┐
                    │  Doctor Login   │
                    └────────┬────────┘
                             │
                    ┌────────▼────────┐
                    │ Doctor Dashboard│
                    │  - Today's Appt │
                    │  - Weekly Chart │
                    │  - Statistics   │
                    └────────┬────────┘
                             │
        ┌────────────────────┼────────────────────┐
        │                    │                    │
┌───────▼───────┐  ┌─────────▼─────────┐  ┌─────▼──────┐
│   Patients    │  │    Calendar       │  │  Reports   │
└───────┬───────┘  └─────────┬─────────┘  └─────┬──────┘
        │                    │                   │
        │                    │                   │
┌───────▼───────┐  ┌─────────▼─────────┐  ┌─────▼──────┐
│ Appointment   │  │  Home Page Info   │  │  Settings  │
│    Slots      │  │  Management       │  │            │
└───────┬───────┘  └─────────┬─────────┘  └─────┬──────┘
        │                    │                   │
        │                    │                   │
        └────────────────────┴───────────────────┘
                             │
                    ┌────────▼────────┐
                    │ Create Slots    │
                    │  - Date         │
                    │  - Time Range   │
                    │  - Duration     │
                    │  - Recurrence   │
                    └────────┬────────┘
                             │
                    ┌────────▼────────┐
                    │ POST /api/      │
                    │ doctors/:id/    │
                    │ appointment-    │
                    │ slots           │
                    └────────┬────────┘
                             │
                    ┌────────▼────────┐
                    │  Slots Saved    │
                    │  to Database    │
                    │  (AVAILABLE)    │
                    └─────────────────┘
```

### Doctor Features:
- ✅ Dashboard with real-time statistics
- ✅ Patient Management (Search, Pagination, Details)
- ✅ Calendar View (Month navigation, Appointment display)
- ✅ Create Appointment Slots (Single/Recurring)
- ✅ View Appointment Slots
- ✅ Manage Home Page Content (Hero, Services, Testimonials, FAQ)
- ✅ Reports & Analytics (Date range, Demographics, Breakdowns)
- ✅ Settings:
  - Profile (with photo upload)
  - Notifications (Events, Delivery, Quiet Hours)
  - Clinic Info (with logo upload)
  - Security (Change Password)

---

## 👩‍💼 Assistant Flow

```
┌─────────────────────────────────────────────────────────────────┐
│                       ASSISTANT DASHBOARD                        │
└─────────────────────────────────────────────────────────────────┘

                    ┌─────────────────┐
                    │ Assistant Login │
                    └────────┬────────┘
                             │
                    ┌────────▼────────┐
                    │Assistant Dashboard│
                    │  - Today's Tasks │
                    │  - Appointments  │
                    │  - Statistics     │
                    └────────┬─────────┘
                             │
        ┌────────────────────┼────────────────────┐
        │                    │                    │
┌───────▼───────┐  ┌─────────▼─────────┐  ┌─────▼──────┐
│ Appointments  │  │    Patients       │  │  Calendar  │
└───────┬───────┘  └─────────┬─────────┘  └─────┬──────┘
        │                    │                   │
        │                    │                   │
┌───────▼───────┐  ┌─────────▼─────────┐  ┌─────▼──────┐
│  Reports      │  │ Communications    │  │  Settings  │
└───────┬───────┘  └─────────┬─────────┘  └─────┬──────┘
        │                    │                   │
        │                    │                   │
        └────────────────────┴───────────────────┘
                             │
                    ┌────────▼────────┐
                    │  Shift Management│
                    │  (Future Feature)│
                    └─────────────────┘
```

### Assistant Features:
- ✅ Dashboard with clinic-specific data
- ✅ Appointment Management (Filter, Search, Statistics)
- ✅ Patient Management (Search, Details, Timeline)
- ✅ Calendar View (Month, Today's Schedule)
- ✅ Reports & Analytics (Date range, Demographics)
- ✅ Communications (Conversations, Chat with patients)
- ✅ Settings:
  - Profile (with photo upload)
  - Notifications
  - Clinic Info (View only)
  - Security (Change Password)

---

## 👨‍💼 Admin Flow

```
┌─────────────────────────────────────────────────────────────────┐
│                          ADMIN DASHBOARD                        │
└─────────────────────────────────────────────────────────────────┘

                    ┌─────────────────┐
                    │   Admin Login   │
                    └────────┬────────┘
                             │
                    ┌────────▼────────┐
                    │  Admin Dashboard│
                    │  - Total Users  │
                    │  - Statistics   │
                    │  - Quick Access │
                    └────────┬────────┘
                             │
                    ┌────────▼────────┐
                    │  User Management│
                    │  (Future)       │
                    └─────────────────┘
```

### Admin Features:
- ✅ Dashboard with system-wide statistics
- ⚠️ User Management (To be implemented)
- ⚠️ System Settings (To be implemented)

---

## 📅 Appointment Booking Flow (Complete)

```
┌─────────────────────────────────────────────────────────────────┐
│                    APPOINTMENT BOOKING FLOW                      │
└─────────────────────────────────────────────────────────────────┘

STEP 1: DOCTOR CREATES SLOTS
┌─────────────────────────────────────────────────────────────┐
│ Doctor Dashboard → Appointment Slots → Create Slots          │
│                                                               │
│ Form:                                                         │
│  - Date: 2024-01-15                                          │
│  - Start Time: 09:00                                          │
│  - End Time: 17:00                                            │
│  - Duration: 30 minutes                                       │
│  - Recurrence: Daily/Weekly/None                             │
│  - Clinic: Main Clinic                                        │
└───────────────────────┬───────────────────────────────────────┘
                        │
                        │ POST /api/doctors/:id/appointment-slots
                        │
        ┌───────────────▼───────────────┐
        │   DoctorsService               │
        │   createAppointmentSlot()      │
        │                                 │
        │   1. Generate time slots        │
        │   2. Handle recurrence         │
        │   3. Save to appointment_slots │
        │      table (status: AVAILABLE) │
        └───────────────┬───────────────┘
                        │
                        │ Response: { slots: [...], totalSlots: 24 }
                        │
        ┌───────────────▼───────────────┐
        │   Database: appointment_slots  │
        │   - id: uuid                   │
        │   - doctorId: uuid             │
        │   - date: 2024-01-15           │
        │   - time: 09:00, 09:30, ...    │
        │   - status: AVAILABLE          │
        └───────────────────────────────┘


STEP 2: PATIENT VIEWS AVAILABLE SLOTS
┌─────────────────────────────────────────────────────────────┐
│ Patient Dashboard → Book Appointment                        │
│                                                               │
│ 1. Select Doctor                                              │
│ 2. Select Clinic                                              │
│ 3. Select Date (Calendar)                                    │
└───────────────────────┬───────────────────────────────────────┘
                        │
                        │ GET /api/doctors/:id/appointment-slots
                        │ ?startDate=2024-01-15&endDate=2024-01-15
                        │
        ┌───────────────▼───────────────┐
        │   DoctorsService               │
        │   getAppointmentSlots()        │
        │                                 │
        │   1. Query appointment_slots   │
        │   2. Filter: status=AVAILABLE  │
        │   3. Filter: date range        │
        │   4. Group by date             │
        └───────────────┬───────────────┘
                        │
                        │ Response: [
                        │   {
                        │     date: "2024-01-15",
                        │     slots: [
                        │       { id, time: "09:00", status: "available" },
                        │       { id, time: "09:30", status: "available" },
                        │       ...
                        │     ]
                        │   }
                        │ ]
                        │
        ┌───────────────▼───────────────┐
        │   Frontend: Display Slots     │
        │   - Show available times       │
        │   - Enable selection           │
        └───────────────────────────────┘


STEP 3: PATIENT BOOKS APPOINTMENT
┌─────────────────────────────────────────────────────────────┐
│ Patient selects:                                             │
│  - Doctor: Dr. John Doe                                      │
│  - Clinic: Main Clinic                                       │
│  - Date: 2024-01-15                                          │
│  - Time: 09:00                                               │
│  - Reason: Regular Checkup                                    │
│  - Notes: (optional)                                          │
└───────────────────────┬───────────────────────────────────────┘
                        │
                        │ POST /api/patients/:id/appointments
                        │ {
                        │   doctorId, clinicId, date, time,
                        │   duration, reason, notes
                        │ }
                        │
        ┌───────────────▼───────────────┐
        │   AppointmentsService          │
        │   create()                     │
        │                                 │
        │   1. Find slot in DB:          │
        │      - doctorId + date + time  │
        │      - status = AVAILABLE      │
        │                                 │
        │   2. If slot found:            │
        │      - Create appointment      │
        │      - Mark slot as BOOKED     │
        │      - Link slot.appointmentId │
        │                                 │
        │   3. If slot not found:        │
        │      - Throw BadRequestException│
        └───────────────┬───────────────┘
                        │
                        │ Response: { appointment: {...} }
                        │
        ┌───────────────▼───────────────┐
        │   Database Updates:            │
        │                                 │
        │   appointments table:          │
        │   - New appointment created     │
        │   - status: PENDING            │
        │                                 │
        │   appointment_slots table:     │
        │   - Slot status: AVAILABLE →    │
        │     BOOKED                      │
        │   - appointmentId: linked       │
        └───────────────────────────────┘


STEP 4: NOTIFICATIONS & CONFIRMATION
┌─────────────────────────────────────────────────────────────┐
│ 1. Patient receives confirmation                            │
│ 2. Doctor sees new appointment in dashboard                 │
│ 3. Slot no longer appears as available                      │
│ 4. Both can view appointment details                        │
└─────────────────────────────────────────────────────────────┘
```

---

## 🔄 System Integration Flow

```
┌─────────────────────────────────────────────────────────────────┐
│                    FRONTEND ↔ BACKEND FLOW                      │
└─────────────────────────────────────────────────────────────────┘

FRONTEND (Next.js)                    BACKEND (NestJS)
──────────────────                    ──────────────────

┌─────────────────┐                  ┌─────────────────┐
│  React Component│                  │   Controller    │
│  (Page/Component)│                  │   (@Controller) │
└────────┬────────┘                  └────────┬────────┘
         │                                    │
         │  User Action                       │
         │  (Click, Submit)                   │
         │                                    │
┌────────▼────────┐                  ┌────────▼────────┐
│  API Client     │                  │   Guard         │
│  (apiClient.ts) │                  │   (@UseGuards)  │
│                 │                  │   - JwtAuthGuard│
│  - baseURL      │                  │   - RolesGuard   │
│  - headers      │                  └────────┬────────┘
│  - auth token   │                           │
└────────┬────────┘                  ┌────────▼────────┐
         │                           │   Service       │
         │  HTTP Request              │   (@Injectable) │
         │  GET/POST/PATCH/DELETE    │                 │
         │───────────────────────────>│  - Business     │
         │                           │    Logic        │
         │                           │  - Database     │
         │                           │    Operations   │
         │                           └────────┬────────┘
         │                                    │
         │                           ┌────────▼────────┐
         │                           │   Repository    │
         │                           │   (TypeORM)     │
         │                           │                 │
         │                           │  - Query DB     │
         │                           │  - Save/Update │
         │                           │  - Delete       │
         │                           └────────┬────────┘
         │                                    │
         │                           ┌────────▼────────┐
         │                           │   PostgreSQL    │
         │                           │   Database      │
         │                           └─────────────────┘
         │                                    │
         │  HTTP Response                     │
         │  { success, data, message }         │
         │<───────────────────────────────────
         │                                    │
┌────────▼────────┐                          │
│  State Update   │                          │
│  (useState)     │                          │
│                 │                          │
│  - Loading      │                          │
│  - Success      │                          │
│  - Error        │                          │
┌────────▼────────┐                          │
│  UI Update      │                          │
│  (Render)       │                          │
└─────────────────┘                          │
```

### API Response Format:
```json
{
  "success": true,
  "data": { ... },
  "message": "Operation successful"
}
```

### Error Response Format:
```json
{
  "success": false,
  "message": "Error message",
  "statusCode": 400
}
```

---

## 📊 Database Schema Flow

```
┌─────────────────────────────────────────────────────────────────┐
│                        DATABASE RELATIONS                        │
└─────────────────────────────────────────────────────────────────┘

users (User Entity)
├── id (PK)
├── role (patient/doctor/assistant/admin)
├── fullName, email, phoneNumber
├── specialty (for doctors)
└── notificationPreferences (JSONB)

    │
    ├───┐
        │
appointments (Appointment Entity)
├── id (PK)
├── patientId (FK → users.id)
├── doctorId (FK → users.id)
├── clinicId (FK → clinics.id)
├── date, time, duration
├── status (pending/confirmed/cancelled/completed)
└── reason, notes

    │
    ├───┐
        │
appointment_slots (AppointmentSlot Entity) ⭐ NEW
├── id (PK)
├── doctorId (FK → users.id)
├── clinicId (FK → clinics.id)
├── date, time, duration
├── status (available/booked/cancelled)
├── appointmentId (FK → appointments.id, nullable)
└── associatedResources (JSONB)

clinics (Clinic Entity)
├── id (PK)
├── name, address, phone, email
├── description
└── operatingHours (JSONB)

medical_records (MedicalRecord Entity)
├── id (PK)
├── patientId (FK → users.id)
├── doctorId (FK → users.id)
├── category, title, description
└── attachments (JSONB)

lab_results (LabResult Entity)
├── id (PK)
├── patientId (FK → users.id)
├── doctorId (FK → users.id)
├── testType, testDate
└── results (JSONB)

prescriptions (Prescription Entity)
├── id (PK)
├── patientId (FK → users.id)
├── doctorId (FK → users.id)
├── medication, dosage, frequency
└── status (active/completed)

messages (Message Entity)
├── id (PK)
├── conversationId (FK → conversations.id)
├── senderId (FK → users.id)
├── receiverId (FK → users.id)
└── content, attachments

conversations (Conversation Entity)
├── id (PK)
├── participants (Many-to-Many → users)
└── lastMessageId, unreadCount

home_page_content (HomePageContent Entity)
├── id (PK)
└── All home page sections (JSONB)
```

---

## 🔐 Security & Authentication Flow

```
┌─────────────────────────────────────────────────────────────────┐
│                    AUTHENTICATION & AUTHORIZATION                │
└─────────────────────────────────────────────────────────────────┘

1. USER LOGIN
   ┌─────────────┐
   │  Frontend    │  POST /api/auth/login
   │  (Login Form)│─────────────────────────┐
   └─────────────┘                          │
                                            │
                                    ┌───────▼───────┐
                                    │  AuthService   │
                                    │  - Validate    │
                                    │  - Hash check  │
                                    │  - Generate JWT │
                                    └───────┬───────┘
                                            │
                                    ┌───────▼───────┐
                                    │  Response:    │
                                    │  { token,     │
                                    │    user,      │
                                    │    role }     │
                                    └───────┬───────┘
                                            │
   ┌─────────────┐                          │
   │  Frontend    │  Store in localStorage
   │  (Store)     │<─────────────────────────┘
   └─────────────┘

2. PROTECTED ROUTE ACCESS
   ┌─────────────┐
   │  Frontend    │  Request with token
   │  (Component)│─────────────────────────┐
   └─────────────┘                          │
                                            │
                                    ┌───────▼───────┐
                                    │  JwtAuthGuard │
                                    │  - Extract    │
                                    │    token      │
                                    │  - Verify JWT │
                                    │  - Get user   │
                                    └───────┬───────┘
                                            │
                                    ┌───────▼───────┐
                                    │  RolesGuard   │
                                    │  - Check role │
                                    │  - Allow/Deny │
                                    └───────┬───────┘
                                            │
                                    ┌───────▼───────┐
                                    │  Controller   │
                                    │  (Handler)    │
                                    └───────────────┘

3. MIDDLEWARE PROTECTION (Frontend)
   ┌─────────────┐
   │  middleware │  Check route
   │  .ts        │─────────────────────────┐
   └─────────────┘                          │
                                            │
                                    ┌───────▼───────┐
                                    │  Check token  │
                                    │  in storage   │
                                    └───────┬───────┘
                                            │
                            ┌───────────────┴───────────────┐
                            │                               │
                    ┌───────▼───────┐             ┌────────▼────────┐
                    │  Token exists │             │  No token       │
                    │  & Valid      │             │                 │
                    └───────┬───────┘             └────────┬────────┘
                            │                             │
                    ┌───────▼───────┐             ┌────────▼────────┐
                    │  Check role   │             │  Redirect to    │
                    │  matches route│             │  /auth/login    │
                    └───────┬───────┘             └─────────────────┘
                            │
                ┌───────────┴───────────┐
                │                       │
        ┌───────▼───────┐     ┌─────────▼────────┐
        │  Role matches │     │  Role mismatch   │
        │  Allow access │     │  Redirect to     │
        └───────────────┘     │  dashboard       │
                              └──────────────────┘
```

---

## 📱 Key User Journeys

### Journey 1: Patient Books First Appointment
```
1. Patient visits home page
2. Clicks "Book Appointment" → Redirected to login
3. Logs in → Redirected to patient dashboard
4. Clicks "Book Appointment"
5. Selects doctor from list
6. Selects clinic
7. Selects date from calendar
8. Sees available time slots (from appointment_slots table)
9. Selects time slot
10. Enters reason and notes
11. Confirms booking
12. Appointment created, slot marked as BOOKED
13. Receives confirmation
14. Appointment appears in "My Appointments"
```

### Journey 2: Doctor Creates Weekly Slots
```
1. Doctor logs in → Dashboard
2. Navigates to "Appointment Slots"
3. Fills form:
   - Date: Today
   - Time: 09:00 - 17:00
   - Duration: 30 minutes
   - Recurrence: Weekly
4. Submits
5. Backend generates slots for next 4 weeks
6. Slots saved to appointment_slots table (status: AVAILABLE)
7. Doctor sees confirmation: "24 slots created"
8. Switches to "View Slots" tab
9. Sees all created slots grouped by date
```

### Journey 3: Patient Views Medical Records
```
1. Patient logs in → Dashboard
2. Clicks "Medical Records"
3. Frontend calls: GET /api/patients/:id/medical-records
4. Backend queries medical_records table
5. Returns records with pagination
6. Patient can:
   - Filter by category
   - Search by keyword
   - View attachments
   - See provider details
```

---

## 🎯 API Endpoints Summary

### Authentication
- `POST /api/auth/register` - Register new user
- `POST /api/auth/login` - Login user
- `POST /api/auth/forgot-password` - Request password reset
- `POST /api/auth/reset-password` - Reset password
- `POST /api/auth/change-password` - Change password
- `GET /api/auth/profile` - Get current user

### Patients
- `GET /api/patients/:id/dashboard` - Dashboard data
- `GET /api/patients/:id/appointments` - List appointments
- `POST /api/patients/:id/appointments` - Book appointment
- `POST /api/patients/:id/appointments/:appointmentId/reschedule` - Reschedule
- `POST /api/patients/:id/appointments/:appointmentId/cancel` - Cancel
- `GET /api/patients/:id/medical-records` - Medical records
- `GET /api/patients/:id/lab-results` - Lab results
- `GET /api/patients/:id/prescriptions` - Prescriptions
- `GET /api/patients/:id/messages` - Messages

### Doctors
- `GET /api/doctors/:id/dashboard` - Dashboard data
- `GET /api/doctors/:id/patients` - List patients
- `GET /api/doctors/:id/appointments` - List appointments
- `POST /api/doctors/:id/appointment-slots` - Create slots ⭐
- `GET /api/doctors/:id/appointment-slots` - Get slots ⭐
- `GET /api/doctors/:id/reports` - Reports
- `PATCH /api/doctors/:id/profile` - Update profile
- `GET /api/doctors/:id/profile/notifications` - Get notifications
- `PATCH /api/doctors/:id/profile/notifications` - Update notifications
- `GET /api/doctors/:id/profile/clinic` - Get clinic info
- `PATCH /api/doctors/:id/profile/clinic` - Update clinic info

### Assistants
- `GET /api/assistants/dashboard` - Dashboard data
- `GET /api/assistants/appointments` - List appointments
- `GET /api/assistants/patients` - List patients
- `GET /api/assistants/reports` - Reports
- `GET /api/assistants/messages` - Messages
- `PATCH /api/assistants/profile` - Update profile

### Common
- `GET /api/users` - List users (filtered by role)
- `GET /api/clinics` - List clinics
- `GET /api/doctors/:id/appointment-slots` - Get available slots

### Home Page
- `GET /api/home-page-content` - Get content (public)
- `PATCH /api/home-page-content` - Update content (protected)

---

## 🔄 Data Flow Example: Complete Appointment Cycle

```
┌─────────────────────────────────────────────────────────────────┐
│              COMPLETE APPOINTMENT CYCLE                         │
└─────────────────────────────────────────────────────────────────┘

DAY 1: Doctor Creates Slots
────────────────────────────
Doctor → Create Slots Form
  ↓
POST /api/doctors/:id/appointment-slots
  ↓
DoctorsService.createAppointmentSlot()
  ↓
Generate 24 slots (09:00-17:00, 30min intervals)
  ↓
Save to appointment_slots table
  ↓
All slots: status = AVAILABLE


DAY 2: Patient Books Appointment
─────────────────────────────────
Patient → Book Appointment Page
  ↓
Select Doctor & Date
  ↓
GET /api/doctors/:id/appointment-slots?startDate=...&endDate=...
  ↓
DoctorsService.getAppointmentSlots()
  ↓
Query: SELECT * FROM appointment_slots 
       WHERE doctorId = ? AND status = 'available' AND date = ?
  ↓
Return available slots
  ↓
Patient sees: 09:00, 09:30, 10:00, ... (available times)
  ↓
Patient selects: 10:00
  ↓
POST /api/patients/:id/appointments
  ↓
AppointmentsService.create()
  ↓
1. Find slot: WHERE doctorId=? AND date=? AND time=? AND status='available'
2. If found:
   - Create appointment (status: PENDING)
   - Update slot: status='booked', appointmentId=appointment.id
3. If not found: Error
  ↓
Response: Appointment created
  ↓
Patient sees confirmation
Doctor sees new appointment in dashboard


DAY 3: Appointment Day
──────────────────────
Doctor → Dashboard
  ↓
Sees appointment at 10:00
  ↓
Can confirm/cancel
  ↓
If confirmed: appointment.status = CONFIRMED
If cancelled: 
  - appointment.status = CANCELLED
  - slot.status = AVAILABLE (slot freed up)


DAY 4: After Appointment
──────────────────────────
Doctor → Complete appointment
  ↓
appointment.status = COMPLETED
  ↓
Doctor can create:
  - Medical Record
  - Lab Result
  - Prescription
  ↓
All linked to patient
  ↓
Patient can view in their dashboard
```

---

## 📝 Notes

### Key Improvements Made:
1. ✅ **Appointment Slots System**: Slots are now properly saved to database
2. ✅ **Slot Availability**: Patients only see available slots
3. ✅ **Slot Booking**: Slots are marked as booked when appointment is created
4. ✅ **File Upload**: Profile pictures and clinic logos can be uploaded
5. ✅ **Export Features**: Reports can be exported as PDF/Excel
6. ✅ **Reschedule**: Patients can reschedule appointments
7. ✅ **Home Page Management**: Doctors can customize home page content

### Future Enhancements:
- ⚠️ Appointment cancellation should free up slots
- ⚠️ Slot deletion by doctor
- ⚠️ Real-time notifications
- ⚠️ Email/SMS notifications
- ⚠️ Admin user management UI
- ⚠️ Advanced analytics

---

**Last Updated**: 2024-01-15
**Version**: 1.0.0

